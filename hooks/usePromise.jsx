/**
 * @function usePromise
 * @description React Hook to wrap a promise with cleanup logic
 * @param {Function} callback
 * @returns {Function} cleanUpFunction()
 * @author Sarat Chandra Ejjapureddi
 */
export default function usePromise(callback) {
  // Your hook implementation here
}
